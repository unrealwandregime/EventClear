# Contributing

Use small reviewed changes, exact atomic arithmetic, deterministic fixtures, and tests for every capital-bearing branch. Never weaken a mainnet startup gate or replace reviewed semantics with title matching.

Run `make lint`, `make typecheck`, and `make test` before opening a change. Solidity changes additionally require fuzz/invariant tests and static analysis. Schema changes are forward-only migrations.
